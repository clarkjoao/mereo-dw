[project]
name = "mereo"
version = "0.1.0"
description = "Ferramentas de mapeamento dos bancos Mereo (SQL Server)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pymssql>=2.3.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
