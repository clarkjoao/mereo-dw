# Cliente real — somente dev local (mereo_tools: discover, schema, drift, seed origem)
MEREO_HOST=sql-prod.mereo.com
MEREO_PORT=8753
MEREO_USER=
MEREO_PASSWORD=

# Simulador in-cluster (dev) — em prod aponta para cliente real via secrets K8s
# Para copiar localmente: kubectl port-forward -n mereo-sqlserver svc/mssql 11433:1433
# e use MSSQL_HOST=localhost
MSSQL_HOST=mssql.mereo-sqlserver.svc.cluster.local
MSSQL_PORT=1433
MSSQL_USER=sa
MSSQL_PASSWORD=MereoSim2026!
